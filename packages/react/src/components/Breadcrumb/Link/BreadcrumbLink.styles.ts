import { styled } from '../../../styles'

export const LinkContainer = styled('a', {
  color: 'inherit',
  textDecoration: 'none',
  cursor: 'pointer',
})

export const CurrentPageItemContainer = styled('span', {
  color: 'inherit',
})
